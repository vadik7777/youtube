---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: application
  annotations:
    kubernetes.io/ingress.class: "system-ingress"
spec:
  rules:
    - host: 3qw.ru
      http:
        paths:
          - pathType: Prefix
            path: /vault-test/
            backend:
              service:
                name: openresty
                port:
                  number: 80
